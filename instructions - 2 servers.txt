

INSTRUCTIONS

Using the previous example as a guide, create an app that has two web servers.
  * 

One that listens on port 7000 and one that listens on port 7500.
  

The one listening on port 7000 will always tell the user something inspiring about the day

The one listening on 7500 will always tell the user something inspiring about their coding skills

Make sure you create a Github repo and commit this code!

  

**Bonus**

  * 

Look for other ways to expand what your server can do. 

As possibilities:
    * 

Generate the inspirational phrases randomly from a list of predefined phrases
    * 

Use the `twitter` package to also return a random tweet